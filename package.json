{
  "name": "hermes-stack",
  "private": true,
  "type": "module",
  "description": "Parallel Node/TS CLI for hermes-stack — runs alongside the bash justfile + .stack/ until it reaches parity.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"scripts/**/*.ts\" \"services/**/*.ts\"",
    "format:check": "prettier --check \"scripts/**/*.ts\" \"services/**/*.ts\""
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "dotenv": "^17.4.2",
    "dotenv-expand": "^13.0.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.9.0",
    "zx": "^8.8.5"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@vitest/coverage-v8": "^4.1.7",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
