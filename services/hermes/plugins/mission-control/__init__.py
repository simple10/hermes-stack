"""mission-control plugin entry point (stub).

Real implementation lands in Task 17. This stub exists so the plugin
loader recognises the directory and the pytest harness can import the
package."""
from __future__ import annotations


def register(ctx) -> None:
    return None
