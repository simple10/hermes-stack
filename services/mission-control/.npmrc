# better-sqlite3 is a native addon; use ignore-scripts on machines that don't need it
# (e.g. CI/CD targeting Cloudflare Workers only). For self-host Docker builds, run
# without --ignore-scripts to compile the native module.
legacy-peer-deps=true
