# pg substrate: single-service profile (dir==service==profile==pg).
SERVICE_RUNNER=docker
SERVICE_KIND=backend
SERVICE_DESC="postgres + pgvector substrate (consumers: honcho, hindsight, litellm)"
# Image pin — tag-class (drift accepted; bump deliberately). Single source
# of truth: no fallback in compose.yaml; this block IS the default.
SERVICE_STACK_ENV='
PG_VERSION=pg18
'
