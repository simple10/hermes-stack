# rabbitmq

`rabbitmq:4.3.0-management` substrate. Used only by `firecrawl` currently
(its nuq queue). Auto-pulled by `SERVICE_REQUIRES=rabbitmq`.

## Lever

```
RABBITMQ_VERSION=4.3.0-management
```
